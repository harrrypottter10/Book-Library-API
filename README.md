Book Library API

A REST API for managing books using FastAPI and MongoDB.

Features

- Create a book
- Get a book by ID
- Get all books
- Update a book
- Delete a book
- MongoDB database integration
- `in_stock` defaults to `false`
- Book year must be greater than 1000
